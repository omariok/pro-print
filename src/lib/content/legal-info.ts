/**
 * Юридические данные, которые не переводятся: номера из ЕГРЮЛ, редакция
 * документов и подрядчики, обрабатывающие данные по поручению. Тексты
 * документов лежат в словарях и берут отсюда значения через fill().
 *
 * Сменился хостинг или почта — поправьте processors здесь, а названия и
 * адреса подрядчиков — в тексте политики и согласия во всех трёх словарях,
 * и поднимите version.
 */
export const legalInfo = {
  inn: "7805794800",
  kpp: "780501001",
  ogrn: "1227800087239",
  site: "pro-print.pro",
  /** Редакция политики, согласия и раздела о cookie. Уходит в письмо с заявкой как доказательство согласия. */
  version: "2026-09-17",
  timewebInn: "7810945525",
  yandexInn: "7736207543",
};

/** Значения для подстановки в юридические тексты словарей. */
export function legalValues(extra: Record<string, string> = {}): Record<string, string> {
  return { ...legalInfo, ...extra };
}
